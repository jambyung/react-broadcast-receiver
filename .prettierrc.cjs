// Even though the values are exactly same as default values,
// it is recommended to set the values
// since the default values can change in future.

module.exports = {
  singleQuote: true,
  jsxSingleQuote: true,
  semi: true,
  useTabs: false,
  tabWidth: 2,
  arrowParens: 'always', // less confusing when using types
  trailingComma: 'all', // eliminates the need of adding comma when moving last line to other places
};
